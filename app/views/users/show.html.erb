<h1>User Profile</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>
    <tr>
      <td><%= @user.name %></td>
      <% if @user.is_admin? %>
      <td>Role : Admin</td>
      <% end %>
    </tr>
</table>
